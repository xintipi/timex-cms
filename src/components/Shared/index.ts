import { withInstall } from '@/utils';

import buttonOperation from './src/ButtonOperation.vue';
import drawerLanguage from './src/DrawerLanguage.vue';

export const ButtonOperation = withInstall(buttonOperation);
export const DrawerLanguage = withInstall(drawerLanguage);
